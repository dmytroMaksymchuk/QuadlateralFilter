from skimage.feature import graycomatrix, graycoprops
import numpy as np

def compute_glcm(image):
    """
    Compute the GLCM of the image.
    :param image: The image to compute the GLCM of.
    :return: The GLCM of the image.
    """
    return graycoprops()
